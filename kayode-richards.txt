https://codepen.io/Kayode94/pen/NWGXOqy?editors=1100
    1. What is Semantic HTML?  
        HTML that gives meaning to our content
    2. What is HTML used for?  
        It is used for the structure of our website, marking up our content
    3. What is an attribute and where do we put it?
        The attribute is a selector used to select specific elements with a specific attribute. They put in the opening tag.     
    4. What is the h1 tag used for? How many times should I use it on a page?
    The h1 tag is used for very important text on the page, usually the title of an aritcle. It should only be used 1 time.
    5. Name two tags that have required attributes
    the <img> tag with the src attribute and the <a> tag with the href attribute 
    6. What do we put in the head of our HTML document? <head> 
    7. What is an id? An ID is a way to mark a specific element in HTML
    8. What elements can I add an id to?  You can add an ID to any element
    9. How many times can I use the same id on a page? once
    10. What is a class? a class is a way to mark HTML elements
    11. What elements can I add a class to? you can add a class to any element
    12. How many times can I use the same class on a page? you can use the same class as many times as you want.
    13. How do I get my link to open in a new tab? target="_blank"
    14. What is the alt attribute in the image tag used for? to turn the image into a link
    15. How do I reference an id? #nameofID
    16. What is the difference between a section and a div the div element is a generic container meant for grouping html elements together. the section element is used to group related elements together.
    17. What is CSS used for? CSS is used for stylinh an HTML page
    18. How to we select an element? Example - every h2 on the page h2{}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one. You might us an ID when you only want to specify an HTML element. You would use the class attribute when you want to want to style a group of elements with the same class name
    20. How do we select classes in CSS? .class{}
    21. How do we select a p element with a single class of “human””? .human p{}
    22. What is a parent child selector? When would this be useful? the parent child selector is used to change the style of an element nested within another element in HTML. This would be useful when you want to access the child of an element but not the parent element.
    23. How do you select all links within a div with the class of sidebar?
    24. What is a pseudo selector? A pseudo selector is used when you may or may not have a set amount of elements but you want to style them individually.
    25. What do we use the change the spacing between lines? line-height
    26. What do we use to change the spacing between letters? letter-spacing
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform
    28. How do I add a 1px border around my div that is dotted and black? div{outline-style, outline-width}
    29. How do I select everything on the page? *{}
    30. How do I write a comment in CSS? /* This is a comment in CSS /*
    31. How do I find out what file I am in, when I am using the command line? the ls command
    32. Using the command line - how do I see a list of files/folders in my current folder? the ls command
    33. How do I remove a file via the command line? Why do I have to be careful with this?  to remove a file you would type rm then the name of the file you want to remove. We have to be careful when using this because there is no warning promt before the deletion occurs. Also once the deltetion occurs it it permenant.
    34. Why should I use version control?  It helps developers all work on one project at the same time. 
    35. How often should I commit to github? Every 20 minutes.
    36. What is the command we would use to push our repo up to github? git push
    37. Walk me through Lambda's git flow. fork the repo>Add your TL as a collaborator on your own forked repo>clone your repo>got to the repo location in your GitCommand by typing cd and the name of the file>type git clone with the link to your repo to create a clone of the repo>create & switch to a new branch by typeing git checkout -b "your-name"> git add . to add any changes made to the file> git commit -m"comment" to add a comment explaining what your changes were to the file>git push to push the newly updated file to the repo.

Stretch Questions

    1. What is the difference between an inline element and a block element? an inline element does not start on a new line, while a block element takes up an entirely new line.
    2. What happens when an element is positioned absolutely? 
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 
